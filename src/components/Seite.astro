---
/**
 * Die ganze Seite. Startseite und Detail-Adressen rendern dasselbe — nur mit
 * einer anderen offenen Ebene. Dadurch liegt hinter jeder Ebene immer das
 * Plakat, egal ob man gescrollt oder die Adresse direkt aufgerufen hat.
 */
import Abschnitt from './Abschnitt.astro';
import Ebene from './Ebene.astro';
import Sprache from './Sprache.astro';
import { INHALT, type Sprache as SpracheTyp } from '../data/inhalt';

interface Props { sprache?: SpracheTyp; offen?: string | null }
const { sprache = 'de', offen = null } = Astro.props;
const { abschnitte, ebenen, fuss, beschriftung } = INHALT[sprache];
---

<main>
  {abschnitte.map((a) => <Abschnitt a={a} />)}

  {/* Der Sprachschalter gehört zum ersten Bildschirm und scrollt mit ihm
      weg. Er steht absolut im Fluss, nicht fest im Fenster — die Sprache
      wählt man beim Eintreten, nicht auf jedem Abschnitt neu. */}
  <Sprache sprache={sprache} pfad={Astro.url.pathname} />
</main>

<footer class="fuss">
  <span>{fuss.wer}</span>
  <span>{fuss.aufkleber}</span>
  <a href={fuss.kontakt.ziel}>{fuss.kontakt.text} →</a>
</footer>

{ebenen.map((e) => <Ebene e={e} offen={e.schluessel === offen} beschriftung={beschriftung} />)}
